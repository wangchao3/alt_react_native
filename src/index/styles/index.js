import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    filters: {
        padding: 10,
        borderStyle: 'solid',
        borderColor: '#eee',
        borderBottomWidth: 1,
        opacity: 0.8,
    }
})
